# Local development environment variables
# Copy this to .env.local and customize for your local setup

# Skip all quota checks and database queries (MUCH FASTER for local dev)
SKIP_QUOTA_CHECK=true

# Optional: Your OpenAI API key (if you want real alt text generation)
# ALTTEXT_OPENAI_API_KEY=sk-...

# Port and host
PORT=4000
HOST=127.0.0.1

# No Supabase needed for local dev when SKIP_QUOTA_CHECK=true
# SUPABASE_URL=...
# SUPABASE_KEY=...

# No auth token required when quota checks are skipped
# ALT_API_TOKEN=...
