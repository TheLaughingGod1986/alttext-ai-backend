module.exports = {
  testMatch: ['<rootDir>/fresh-stack/tests/**/*.(test|spec).js'],
  testEnvironment: 'node',
  roots: ['<rootDir>/fresh-stack/tests'],
  moduleDirectories: ['node_modules', '<rootDir>/fresh-stack'],
  collectCoverage: false
};
